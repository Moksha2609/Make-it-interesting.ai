// This utility helps track repetition for students
module.exports = function trackRepetition(studentId, topic) {
    // Pseudo logic to track and replay lessons
    // Returns whether the topic should be repeated
    return Math.random() > 0.5;  // Simplified for demo
};
